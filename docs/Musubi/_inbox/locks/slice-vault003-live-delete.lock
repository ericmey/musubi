owner: cowork-tama
branch: slice/vault003-live-delete
claimed: 2026-07-15
worktree: /tmp/vault003/wt
base: 9a9f209 (live origin/main, no drift on this lane)
scope: VAULT-003 / H12 P0 — live VaultWatcher delete event through
  canonical LifecycleTransitionCoordinator archive transition
  (Issue #552). Tests-first slice; zero src in this commit.
  10-bullet Test Contract at tests/vault/test_vault003_live_delete.py:
  8 RED discriminating + 2 GREEN preservation guards. Implementation
  commits follow (CuratedPlane.find_by_vault_path typed public
  method; VaultWatcher.__init__ required coordinator parameter;
  _handle_deleted canonical-transition wiring with the idempotency
  carve-out for archived->archived only).
out-of-scope:
  - VAULT-001 periodic ghost reconciliation (closed, Issue #446)
  - VAULT-002 boot-scan path handling
  - H13 frontmatter fidelity
  - broad move/rename redesign
constraints:
  - SEPARATE worktree from auth001/life009/ret012/dq001
  - tests-first per AGENTS.md Test Contract Closure Rule
  - exact-head CI green before marking ready
  - independent review by Yua; no self-merge
risk:
  - in-handler retry on version_fence_violation is FORBIDDEN per
    Yua approval — risk is unbounded recursion. log warning + bail
    instead; rely on later filesystem/reconcile events for retry.
binding-from-review:
  - find_by_vault_path lives on CuratedPlane as typed public method;
    watcher must NOT scroll Qdrant directly.
  - On version_fence_violation or any non-idempotent transition
    error: do NOT retry in same delete handler.