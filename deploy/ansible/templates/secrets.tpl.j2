# Rendered to {{ musubi_config_dir }}/secrets.tpl. Contains NO secrets — only
# op:// references resolved at service start by `op run` via 1Password Connect.
# Safe at rest. See musubi.service.j2.
JWT_SIGNING_KEY=op://Harem World/musubi-jwt-signing-key/credential
QDRANT_API_KEY=op://Harem World/musubi-qdrant-auth/credential
